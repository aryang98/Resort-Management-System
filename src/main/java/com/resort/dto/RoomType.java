// Declaring the package where the RoomType enum resides
package com.resort.dto;

// Enum representing different types of rooms available
public enum RoomType {

	// Two types of rooms: SingleRoom and DoubleRoom
	SingleRoom, // Enum constant representing a single room type
	DoubleRoom // Enum constant representing a double room type
}
