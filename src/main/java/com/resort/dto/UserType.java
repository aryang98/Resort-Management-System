package com.resort.dto;

// Enum representing different types of users
public enum UserType {
	// Enum constants representing different user types

	// User type 'User'
	User,

	// User type 'Admin'
	Admin

}
